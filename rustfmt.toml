# Workspace-wide rustfmt config. Keep this in sync with what `cargo fmt --all`
# enforces in CI.
edition = "2024"
max_width = 100
fn_call_width = 80
newline_style = "Unix"
use_field_init_shorthand = true
